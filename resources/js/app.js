import './bootstrap';

// Import Alpine
import Alpine from 'alpinejs';

// Menjadikan Alpine variabel global (agar bisa dipanggil di console browser jika perlu debugging)
window.Alpine = Alpine;

// Jalankan Alpine
Alpine.start();